[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
Django = ">=4.2,<5.0"
django-cors-headers = ">=4.4.0"
django-distill = ">=3.2.7"
dj-database-url = ">=2.1.0"
psycopg2-binary = ">=2.9.10"
gunicorn = ">=20.1.0"
strawberry-graphql = {extras = ["django"], version = ">=0.209.0"}
pydantic = ">=2.9.0"
requests = ">=2.32.3"
cloudflare = ">=2.19.0"
httpx = ">=0.27.0"
redis = ">=5.0.8"
django-redis = ">=5.4.0"
python-dotenv = ">=1.0.0"
beautifulsoup4 = ">=4.12.3"
croniter = ">=2.0.7"
PyYAML = ">=6.0.2"

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
ruff = ">=0.8.0"
pre-commit = ">=3.0.0"
mypy = ">=1.0.0"

[requires]
python_version = "3.12"
