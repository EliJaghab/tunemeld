{
  "name": "tunemeld-frontend",
  "version": "1.0.0",
  "description": "TuneMeld Frontend TypeScript Application",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
