{
  "name": "tunemeld-frontend",
  "version": "1.0.0",
  "description": "TuneMeld Frontend TypeScript Application",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js && tsc --watch",
    "dev": "node build.js && tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "tsc-alias": "^1.8.10"
  }
}
